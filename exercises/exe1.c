// Write a program to calculate the area of a square(side give)
#include <stdio.h>
int main()
{
    int side;
    printf("Enter the length of the square: ");
    scanf("%d", &side);
    printf("Area of a square is: %d\n", side*side);
}