//Arrays -> collection of similar data types stored in contiguous memory locations
#include <stdio.h>
int main()
{
    int marks[4] = {1,2,3,4};
    //Input
    // scanf("%d", &marks[0]);

    //Output
    //printf("%d\n", marks[0]);
    
    //Runtime error occurs when u are accessing more than the allocated memory like marks[3] which doesnt exist
    



    return 0;
}