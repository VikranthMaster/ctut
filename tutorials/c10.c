// Control Instructions
// Used to determine flow of program

/*
    A. Sequence Control (we are writing sequence control from all this time)
    B. Decision Control (if else etc...)
    C. Loop Control(while loop for loop etc....)
    D. Case Control(case)
*/
// ----------------------------------------------------------------

// Operators
// True -> 1, False -> 0
/*
    A. Arithmetic Operators (+, -, *, /, %)
    B. Assignment Operators (=, +=, -=, *=, /=, %=)
    C. Comparison or Relational Operators (==,!=, >, <, >=, <=)
    D. Logical Operators (&&, ||,!)
    E. Bitwise Operators (&, |, ~, ^, <<, >>)
    F. Ternary Operators (? :) (conditional operator)
*/
